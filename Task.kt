// Task.kt
// Mark Cuizon
data class Task(val id: Int, val description: String, var isComplete: Boolean = false)  // For a task, we will need an id, description, and a boolean isComplete (default is false). This works like a dictionary.
